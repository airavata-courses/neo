apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-service-deploy
spec:
  selector:
    matchLabels:
      app: data-service
  replicas: 2
  template:
    metadata:
      labels:
        app: data-service

    spec:
      containers:
      - name: data-service
        imagePullPolicy: Always
        image: neoairavataproject/data-service:2.3
        resources:
          requests:
            cpu: 500m
            memory: 800Mi
        ports:
        - containerPort: 8082
      restartPolicy: Always
